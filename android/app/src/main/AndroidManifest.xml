<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- Dual-SIM / OEM SmsManager paths call getGroupIdLevel1() which needs this. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 11+: Kit2 picker discovers Core via SEND_DATA; without this intent query,
         getCurrentlySelectedApp() can be null and inbound Kit2 traffic is silently dropped. -->
    <queries>
        <package android:name="coredevices.coreapp" />
        <package android:name="com.getpebble.android.basalt" />
        <package android:name="com.getpebble.android" />
        <package android:name="io.rebble.cobble" />
        <provider android:authorities="com.getpebble.android.provider.basalt" />
        <intent>
            <action android:name="io.rebble.pebblekit2.SEND_DATA_TO_WATCH" />
        </intent>
        <intent>
            <action android:name="io.rebble.pebblekit2.RECEIVE_DATA_FROM_WATCH" />
        </intent>
    </queries>

    <application
        android:name=".EzSosApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EzSos">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ezsos" android:host="settings" />
                <data android:scheme="ezsos" android:host="configure" />
                <data android:scheme="ezsos" android:host="sos" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.FirstRunActivity"
            android:exported="false" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.EventLogActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.FullScreenAlarmActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.EzSos"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <!-- Classic PebbleKit AppMessage receive. -->
        <receiver
            android:name=".pebble.EzSosPebbleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.getpebble.action.app.RECEIVE" />
            </intent-filter>
        </receiver>

        <!-- PebbleKit 2: Core binds this while EZ SOS is open on the watch. -->
        <service
            android:name=".pebble.EzSosPebbleKit2Service"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="io.rebble.pebblekit2.RECEIVE_DATA_FROM_WATCH" />
            </intent-filter>
        </service>

        <receiver
            android:name=".pebble.PebbleConnectionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.getpebble.action.PEBBLE_CONNECTED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
